---
import languages from '../../../../i18n/languages';
import { groupPagesByLang } from '../../../../utils/groupPagesByLang';

export async function getStaticPaths() {
	const allPages = await Astro.glob('../../../../../data/docs/1.0/en/**/*.md');
	const pagesByLang = groupPagesByLang(allPages);
	const paths = [];
	return paths;
}
const { fallback } = Astro.params;
const englishPages = await Astro.glob('../../../../../data/docs/1.0/en/**/*.md');
const { Content } = englishPages.find((p) => {
	return p.url.endsWith(fallback)
});
---

<Content />