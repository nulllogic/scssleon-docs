---
title: Validation
description: Give textual form controls like `<input>`s and `<textarea>`s an upgrade with custom styles, sizing, focus states, and more.
category: forms
toc: true
---

import {Raw, Snippet} from "/src/components/snippet"

## Basic &lt;input&gt;

<Snippet title={'Validation 1'} preview={true}>
  <Fragment slot="html">
    <Raw file={file} line_start={16} line_end={23}>
      ```
      <div class="form">
        <div class="field is-invalid">
          <label for="input1" class="label">Email address:</label>
          <input type="email" class="input"
                 id="input1" placeholder="name@example.com"/>
          <p class="invalid-feedback">Please provide a valid email.</p>
        </div>
      </div>
      ```
    </Raw>
  </Fragment>
  <Fragment slot="scss">
    <Raw file={file} line_start={30} line_end={39}>
      ```
      @use '~/styles/app.scss' as app;

      $form: app.get-theme(app.$theme, 'components.form');

      @include app.generate-component(
        $form,
        'form',
        app.$config,
        app.$theme
      );
      ```
    </Raw>
  </Fragment>
</Snippet>