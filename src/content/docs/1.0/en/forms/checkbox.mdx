---
title: Checkbox
description: Give textual form controls like `<input>`s and `<textarea>`s an upgrade with custom styles, sizing, focus states, and more.
category: forms
toc: true
---
import {Raw, Snippet} from "/src/components/snippet"

## Example
<Snippet title={'Checkbox 1'} preview={true}>
  <Fragment slot="html">
    <Raw file={file} line_start={14} line_end={29}>
    ```
    <div class="form">
      <div class="field">
        <div class="inline">
          <input class="checkbox" type="checkbox" id="input1" value=""/>
          <label class="label" for="input1">
            Default checkbox
          </label>
        </div>
        <div class="inline">
          <input class="checkbox" type="checkbox" id="input2" checked/>
          <label class="label" for="input2">
            Default checked checkbox
          </label>
        </div>
      </div>
    </div>
    ```
    </Raw>
  </Fragment>
  <Fragment slot="scss">
    <Raw file={file} line_start={36} line_end={44}>
    ```
    @use '~/styles/app.scss' as app;

    $form: app.get-theme(app.$theme, 'components.form');
    @include app.generate-component(
      $form,
      'form',
      app.$config,
      app.$theme
    );
    ```
    </Raw>
  </Fragment>
</Snippet>

## Indeterminate
<Snippet title={'Checkbox 2'} preview={true}>
  <Fragment slot="html">
    <Raw file={file} line_start={55} line_end={65}>
    ```
    <div class="form">
      <div class="field">
        <div class="inline">
          <input class="checkbox" type="checkbox" id="input1" value=""/>
          <label class="label" for="input1">
            Default checkbox
          </label>
        </div>
        <p class="text-muted-foreground text-sm">By clicking this checkbox, you agree to the terms and conditions.</p>
      </div>
    </div>
    ```
    </Raw>
  </Fragment>
  <Fragment slot="scss">
    <Raw file={file} line_start={72} line_end={79}>
    ```
    @use '~/styles/app.scss' as app;

    @include app.generate-component(
      app.get-theme(app.$theme, 'components.form'),
      'form',
      app.$config,
      app.$theme
    );
    ```
    </Raw>
  </Fragment>
  <Fragment slot="js">
    <Raw file={file} line_start={86} line_end={86}>
      ```
      document.getElementById('input1').indeterminate = true;
      ```
    </Raw>
  </Fragment>
</Snippet>
