---
title: Accordion
description: A web component that expands and collapses content sections, much like an accordion's bellows, for a tidy and user-friendly experience
category: components
toc: true
schema: true
---
import {Snippet} from "~/components/snippet"

## Example
<Snippet frame_url={'components/accordion/1'} preview={true}>
    <Fragment slot={'html'}>
        ```html
        <div class="accordion">
            <div class="item">
                <button type="button" aria-expanded="false" aria-controls="accordion-panel-1" id="accordion-header-1"
                        id="accordion1id">
                        <span class="accordion-title">
                          Personal Information
                          <span class="accordion-icon"></span>
                        </span>
                </button>
                <div class="content" id="accordion-panel-1"
                     aria-labelledby="accordion-header-1"
                     hidden>333
                </div>
            </div>
        </div>
        ```
    </Fragment>
    <Fragment slot={'scss'}>
        ```scss
        @use "sass:list";
        @use "sass:string";
        @use "sass:meta";
        @use "sass:map";

        @use "~/src/styles/app.scss" as app;
        @use "@nulllogic/scssleon/scss/mixins.scss" as mixins;
        @use "@nulllogic/scssleon/scss/functions.scss" as functions;

        $accordion: functions.get-theme(app.$theme, 'components.accordion');

        @include mixins.generate-component(
        $accordion,
        "accordion",
        app.$config,
        app.$theme
        );
        ```
    </Fragment>
    <Fragment slot={'js'}>
        ```js

        ```
    </Fragment>
</Snippet>

## Example 2
<Snippet frame_url={'components/accordion/2'} preview={true}>
    <Fragment slot={'html'}>
        ```html
        <div class="accordion">
            <div class="item">
                <div class="title">123</div>
                <div class="content">333</div>
            </div>
        </div>
        ```
    </Fragment>
    <Fragment slot={'scss'}>
        ```scss
        @use "sass:list";
        @use "sass:string";
        @use "sass:meta";
        @use "sass:map";

        @use "~/src/styles/app.scss" as app;
        @use "@nulllogic/scssleon/scss/mixins.scss" as mixins;
        @use "@nulllogic/scssleon/scss/functions.scss" as functions;

        $accordion: functions.get-theme(app.$theme, 'components.accordion');

        @include mixins.generate-component(
            $accordion,
            "accordion",
            app.$config,
            app.$theme
        );
        ```
    </Fragment>
    <Fragment slot={'js'}>
        ```js
        console.log(123);
        ```
    </Fragment>
</Snippet>

```html
123123
```