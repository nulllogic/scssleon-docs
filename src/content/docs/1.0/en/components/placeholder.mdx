---
title: Placeholder
description: A placeholder component is like a digital sketchpad, providing a temporary space for users to fill in with their own content. It's a handy tool that helps users understand what kind of information is expected, without feeling overwhelmed by a blank page. With a placeholder component, you can easily guide users through your website, without having to worry about getting lost in the digital wilderness.
category: components
toc: true
---
import {Raw, Snippet} from "/src/components/snippet"

## About

Placeholders can be used to enhance the experience of your application. They're built only with HTML and CSS, meaning you don't need any JavaScript to create them. You will, however, need some custom JavaScript to toggle their visibility. Their appearance, color, and sizing can be easily customized with our utility classes.

## Example

In the example below, we take a typical card component and recreate it with placeholders applied to create a "loading card". Size and proportions are the same between the two.
<Snippet title={'Placeholder 1'} preview={true}>
    <Fragment slot="html">
      <Raw file={file} line_start={20} line_end={26}>
        ```
        <div class="placeholder" aria-hidden="true">
            <span class="el"></span>
            <span class="el"></span>
            <span class="el"></span>
            <span class="el"></span>
            <span class="el"></span>
        </div>
        ```
      </Raw>
    </Fragment>
    <Fragment slot="scss">
      <Raw file={file} line_start={33} line_end={42}>
        ```
        @use '~/styles/app.scss' as app;

        $placeholder: app.get-theme(app.$theme, 'components.placeholder');

        @include app.generate-component(
            $placeholder,
            'placeholder',
            app.$config,
            app.$theme
        );
        ```
      </Raw>
    </Fragment>
</Snippet>

### Width

You can change the `width` through grid column classes, width utilities, or inline styles.

<Snippet title={'Placeholder 2'} preview={true}>
    <Fragment slot="html">
      <Raw file={file} line_start={56} line_end={60}>
        ```
        <div class="placeholder" aria-hidden="true">
            <span class="el"></span>
            <span class="el"></span>
            <span class="el" style="width: 25%;"></span>
        </div>
        ```
      </Raw>
    </Fragment>
    <Fragment slot="scss">
      <Raw file={file} line_start={67} line_end={76}>
        ```
        @use '~/styles/app.scss' as app;

        $placeholder: app.get-theme(app.$theme, 'components.placeholder');

        @include app.generate-component(
            $placeholder,
            'placeholder',
            app.$config,
            app.$theme
        );
        ```
      </Raw>
    </Fragment>
</Snippet>

### Animation

Animate placeholders with `.glow` or `.wave` to better convey the perception of something being _actively_ loaded.

<Snippet title={'Placeholder 3'} preview={true}>
    <Fragment slot="html">
      <Raw file={file} line_start={90} line_end={94}>
        ```
        <p class="placeholder glow">
            <span class="el"></span>
            <span class="el"></span>
            <span class="el"></span>
        </p>
        ```
      </Raw>
    </Fragment>
    <Fragment slot="scss">
      <Raw file={file} line_start={101} line_end={110}>
        ```
        @use '~/styles/app.scss' as app;

        $placeholder: app.get-theme(app.$theme, 'components.placeholder');

        @include app.generate-component(
            $placeholder,
            'placeholder',
            app.$config,
            app.$theme
        );
        ```
      </Raw>
    </Fragment>
</Snippet>

<Snippet title={'Placeholder 4'} preview={true}>
    <Fragment slot="html">
      <Raw file={file} line_start={120} line_end={124}>
        ```
        <p class="placeholder wave">
            <span class="el"></span>
            <span class="el"></span>
            <span class="el"></span>
        </p>
        ```
      </Raw>
    </Fragment>
    <Fragment slot="scss">
      <Raw file={file} line_start={131} line_end={140}>
        ```
        @use '~/styles/app.scss' as app;

        $placeholder: app.get-theme(app.$theme, 'components.placeholder');

        @include app.generate-component(
            $placeholder,
            'placeholder',
            app.$config,
            app.$theme
        );
        ```
      </Raw>
    </Fragment>
</Snippet>

## Sass
