---
title: Card
description: Card
category: components
toc: true
---

import {Snippet} from "/src/components/snippet"

## Examples

<Snippet frame_url={'components/card/1'} preview={true}>
    <Fragment slot="html">
        ```html
        <div class="card">
            123
        </div>
        ```
    </Fragment>
    <Fragment slot="scss">
        ```scss
        @use "sass:list";
        @use "sass:string";
        @use "sass:meta";
        @use "sass:map";

        @use "~/src/styles/app.scss" as app;
        @use "~/src/styles/scss/mixins.scss" as mixins;
        @use "~/src/styles/scss/functions.scss" as functions;

        $card: functions.get-theme(app.$theme, 'components.card');

        @include mixins.generate-component(
            $card,
            "card",
            app.$config,
            app.$theme
        );
        ```
    </Fragment>
</Snippet>

