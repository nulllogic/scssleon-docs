---
import toc from "./toc/index.astro";
---

<main class="main">
  <slot />
</main>

<style lang="scss">
  @use 'sass:list';
  @use 'sass:meta';
  @use 'sass:map';

  @use '~/styles/app.scss' as app;

  $main: (
    /*display: grid,*/
    /*grid-template-columns: 2fr 1fr,*/
  );

  @include app.generate-component(
      $main,
      'main',
      app.$config,
      app.$theme,
  );

</style>