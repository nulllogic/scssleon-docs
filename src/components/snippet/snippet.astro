---
import { extractor } from './utils/';
// HTML
const html_rendered = (await Astro.slots.render('html'));
// SCSS
const scss_rendered = (await Astro.slots.render('scss'));
// JS
const js_rendered = (await Astro.slots.render('js'));

const {
  title = '',
  preview = false,
  file = null,
  min_height = '',
  inline = false,
  single = false,
  multi_line = true,
  lang = 'js',
} = Astro.props;

const {
  html = null,
  scss = null,
  js = null,
  code = null,
} = await extractor(file, title);

import Button from './sub/button.astro';
import Code from './sub/code.astro';
import { component_classes } from './utils/classes_generator.ts';

const lines = code ? code.split(/\r\n|\r|\n/).length : null;
const scss_generated = scss ? component_classes(scss) : null;
scss_generated.then((value) => {
    console.log(value);
    // Expected output: 123
});
---
<div class:list={['snippet', { 'single': single }]}>
  {(!single || (lines && lines > 1)) && (
          <div class="top">
              <div class="nav">
                  <div class="left">
                      <div class="title"
                           set:html={Astro.props.title.replace(/\*(.*?)\*/g,
                             '<span class="bold">$1</span>')}>
                      </div>
                  </div>
                  <div class="right">
                    {!code && (
                            <div class="btn-group separated" role="group"
                                 aria-label="Basic example"
                                 style="--xii-gap: .425rem;">
                                <Button small icon={'outline'}/>
                                <Button small icon={'color_scheme'}/>
                            </div>
                    )}
                  </div>
              </div>
          </div>
  )}

    <div class="middle">
      {preview && (
              <div class="preview">
                  <xii-preview>
                      <template>
                          <div set:html={html}></div>
                          {scss && (
                          <style set:html={scss_generated} />
                          )}
                          {js && (
                          <script set:html={js} />
                          )}
                      </template>
                  </xii-preview>
              </div>
      )}
      {code && (
              <Code code={code} lang={lang} single={single}
                    oneline={lines && lines === 1}/>
              <div class:list={[
                'btn_wrapper',
                { 'vertical': lines && lines === 1 }]}>
                  <Button small={lines && lines === 1} icon={'clipboard'}/>
              </div>
      )}
    </div>
  {!single && (
          <div class="bottom">
              <div class="nav">
                  <div class="left">
                      <div class="btn-group" role="group"
                           aria-label="Basic example">
                          <Button>HTML</Button>
                          <Button>SCSS</Button>
                          <Button>JS</Button>
                      </div>
                  </div>
                  <div class="right">
                      <div class="btn_wrapper">
                          <Button small icon="clipboard">Right</Button>
                      </div>
                  </div>
              </div>
              <div class="tabs">
                  <div class="tab">
                      <Code code={html} lang={'html'}
                            show_line_numbers={true}/>
                  </div>
                  <div class="tab">
                      <Code code={scss} lang={'scss'}
                            show_line_numbers={true}/>
                  </div>
                  <div class="tab">
                      <Code code={js} lang={'js'} show_line_numbers={true}/>
                  </div>
              </div>
          </div>
  )}
</div>

<style lang="scss">
  @use 'sass:map';
  @use 'sass:string';

  @use '~/styles/app.scss' as app;

  $snippet-top: (
    background: #0d1117,
    border-radius: .425rem .425rem 0 0,
    padding: .525rem .825rem,
    border-bottom: 1px solid #333434,
    min-height: 46px,
    align-items: center,
    display: flex,
    _subclasses : (
      '.nav' : (
        width: 100%,
        justify-content : space-between,
        _subclasses : (
          '.title' : (
            _subclasses : (
              ':global(.bold)' : (
                font-weight: 500,
                display: inline-block,
                padding: 0 .25rem 0 0,
              )
            )
          )
        )
      ),
    )
  );

  $snippet-middle: (
    border-radius: 0 0 0.425rem 0.425rem,
    position: relative,
    _subclasses : (
      '.preview' : (
        padding: 1rem .825rem,
        border-bottom: 1px solid #333434,
        background: var(--background),
        _colors : (
          light : (
            /*color: rgb(229, 236, 226),*/
            background : string.unquote('rgba(55, 241, 14, 0.09) linear-gradient(135deg, rgba(219, 226, 216, 0.45) 10%, rgba(0, 0, 0, 0) 0, #0000 50%, rgba(219, 226, 216, 0.45) 0, rgba(219, 226, 216, 0.45) 60%, rgba(0, 0, 0, 0) 0, rgba(0, 0, 0, 0)) repeat 100% /7.07px 7.07px'),
          ),
          dark : (
            background : string.unquote('rgba(19, 38, 81, 0.1) linear-gradient(135deg, rgba(19, 83, 14, 0.45) 10%, rgba(0, 0, 0, 0) 0, #0000 50%, rgba(19, 83, 14, 0.45) 0, rgba(19, 83, 14, 0.45) 60%, rgba(0, 0, 0, 0) 0, rgba(0, 0, 0, 0)) repeat 100% /7.07px 7.07px'),
          )
        )
      ),
      '> .btn_wrapper' : (
        position: absolute,
        top: 8px,
        right: 8px,
        display: inline-block,
        _subclasses : (
          '&.vertical' : (
            top: 50%,
            transform: translateY(-50%)
          )
        )
      )
    )
  );


  $snippet-bottom: (
    background: #0d1117,
    border-radius: 0 0 0.425rem 0.425rem,
    _subclasses : (
      '.nav' : (
        padding: .525rem .825rem,
        justify-content : space-between,
        border-bottom: 1px solid #333434,
      ),
      '.tabs' : (
        _subclasses : (
          '.tab' : (
            _subclasses : (
              '&:first-child' : (
                display: block,
              )
            ),
            display: none
          )
        )
      )
    )
  );

  $snippet: (
    position: relative,
    z-index: 1,
    text-align: left,
    padding: 0,
    display: block,
    margin: 0,
    border: 1px solid #3d444d,
    border-radius: .425rem,
    _subclasses : (
      '.top' : $snippet-top,
      '.middle' : $snippet-middle,
      '.bottom' : $snippet-bottom,
      '&.single' : (
        _subclasses : (
          '.middle' : (
            border-radius: .425rem,
          )
        )
      )
    )
  );

  @include app.generate-component(
    $snippet,
    '.snippet',
    app.$config,
    app.$theme
  );
</style>

<script client:load>
  customElements.get('xii-preview') === undefined &&
  window.customElements.define('xii-preview', class extends HTMLElement {
    constructor () {
      super();

      // 🥷创建 Shadow DOM
      const shadow = this.attachShadow({ mode: 'open' });

      shadow.innerHTML = this.querySelector('template')?.innerHTML;
    }

    connectedCallback () {

    }
  });
</script>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const snippets = document.querySelectorAll('.snippet');
    snippets.forEach((snippet) => {

      const top_buttons = snippet.querySelectorAll('.top .btn');

      top_buttons.forEach((button, index) => {
        switch (index) {
          // Outline button click
          case 0 :
            button.addEventListener('click', (e) => {
              e.preventDefault();
              snippet.querySelector('.preview > xii-preview')?.
                shadowRoot.
                querySelector('slot[name="html"]').
                assignedElements()[0].classList.toggle('outline');
            });
            break;
        }
      });

      const middle_buttons = snippet.querySelectorAll('.middle .btn');

      middle_buttons.forEach((button, index) => {
        console.log(button);
      });
    });
  });
</script>

<style lang="scss">
  // @formatter:off
    :global(.outline) {
        :global(body) { outline: 1px solid #2980b9 !important; }
        :global(article) { outline: 1px solid #3498db !important; }
        :global(nav) { outline: 1px solid #0088c3 !important; }
        :global(aside) { outline: 1px solid #33a0ce !important; }
        :global(section) { outline: 1px solid #66b8da !important; }
        :global(header) { outline: 1px solid #99cfe7 !important; }
        :global(footer) { outline: 1px solid #cce7f3 !important; }
        :global(h1) { outline: 1px solid #162544 !important; }
        :global(h2) { outline: 1px solid #314e6e !important; }
        :global(h3) { outline: 1px solid #3e5e85 !important; }
        :global(h4) { outline: 1px solid #449baf !important; }
        :global(h5) { outline: 1px solid #c7d1cb !important; }
        :global(h6) { outline: 1px solid #4371d0 !important; }
        :global(main) { outline: 1px solid #2f4f90 !important; }
        :global(address) { outline: 1px solid #1a2c51 !important; }
        :global(div) { outline: 1px solid #036cdb !important; }
        :global(p) { outline: 1px solid #ac050b !important; }
        :global(hr) { outline: 1px solid #ff063f !important; }
        :global(pre) { outline: 1px solid #850440 !important; }
        :global(blockquote) { outline: 1px solid #f1b8e7 !important; }
        :global(ol) { outline: 1px solid #ff050c !important; }
        :global(ul) { outline: 1px solid #d90416 !important; }
        :global(li) { outline: 1px solid #d90416 !important; }
        :global(dl) { outline: 1px solid #fd3427 !important; }
        :global(dt) { outline: 1px solid #ff0043 !important; }
        :global(dd) { outline: 1px solid #e80174 !important; }
        :global(figure) { outline: 1px solid #ff00bb !important; }
        :global(figcaption) { outline: 1px solid #bf0032 !important; }
        :global(table) { outline: 1px solid #00cc99 !important; }
        :global(caption) { outline: 1px solid #37ffc4 !important; }
        :global(thead) { outline: 1px solid #98daca !important; }
        :global(tbody) { outline: 1px solid #64a7a0 !important; }
        :global(tfoot) { outline: 1px solid #22746b !important; }
        :global(tr) { outline: 1px solid #86c0b2 !important; }
        :global(th) { outline: 1px solid #a1e7d6 !important; }
        :global(td) { outline: 1px solid #3f5a54 !important; }
        :global(col) { outline: 1px solid #6c9a8f !important; }
        :global(colgroup) { outline: 1px solid #6c9a9d !important; }
        :global(button) { outline: 1px solid #da8301 !important; }
        :global(datalist) { outline: 1px solid #c06000 !important; }
        :global(fieldset) { outline: 1px solid #d95100 !important; }
        :global(form) { outline: 1px solid #d23600 !important; }
        :global(input) { outline: 1px solid #fca600 !important; }
        :global(keygen) { outline: 1px solid #b31e00 !important; }
        :global(label) { outline: 1px solid #ee8900 !important; }
        :global(legend) { outline: 1px solid #de6d00 !important; }
        :global(meter) { outline: 1px solid #e8630c !important; }
        :global(optgroup) { outline: 1px solid #b33600 !important; }
        :global(option) { outline: 1px solid #ff8a00 !important; }
        :global(output) { outline: 1px solid #ff9619 !important; }
        :global(progress) { outline: 1px solid #e57c00 !important; }
        :global(select) { outline: 1px solid #e26e0f !important; }
        :global(textarea) { outline: 1px solid #cc5400 !important; }
        :global(details) { outline: 1px solid #33848f !important; }
        :global(summary) { outline: 1px solid #60a1a6 !important; }
        :global(command) { outline: 1px solid #438da1 !important; }
        :global(menu) { outline: 1px solid #449da6 !important; }
        :global(del) { outline: 1px solid #bf0000 !important; }
        :global(ins) { outline: 1px solid #400000 !important; }
        :global(img) { outline: 1px solid #22746b !important; }
        :global(iframe) { outline: 1px solid #64a7a0 !important; }
        :global(embed) { outline: 1px solid #98daca !important; }
        :global(object) { outline: 1px solid #00cc99 !important; }
        :global(param) { outline: 1px solid #37ffc4 !important; }
        :global(video) { outline: 1px solid #6ee866 !important; }
        :global(audio) { outline: 1px solid #027353 !important; }
        :global(source) { outline: 1px solid #012426 !important; }
        :global(canvas) { outline: 1px solid #a2f570 !important; }
        :global(track) { outline: 1px solid #59a600 !important; }
        :global(map) { outline: 1px solid #7be500 !important; }
        :global(area) { outline: 1px solid #305900 !important; }
        :global(a) { outline: 1px solid #ff62ab !important; }
        :global(em) { outline: 1px solid #800b41 !important; }
        :global(strong) { outline: 1px solid #ff1583 !important; }
        :global(i) { outline: 1px solid #803156 !important; }
        :global(b) { outline: 1px solid #cc1169 !important; }
        :global(u) { outline: 1px solid #ff0430 !important; }
        :global(s) { outline: 1px solid #f805e3 !important; }
        :global(small) { outline: 1px solid #d107b2 !important; }
        :global(abbr) { outline: 1px solid #4a0263 !important; }
        :global(q) { outline: 1px solid #240018 !important; }
        :global(cite) { outline: 1px solid #64003c !important; }
        :global(dfn) { outline: 1px solid #b4005a !important; }
        :global(sub) { outline: 1px solid #dba0c8 !important; }
        :global(sup) { outline: 1px solid #cc0256 !important; }
        :global(time) { outline: 1px solid #d6606d !important; }
        :global(code) { outline: 1px solid #e04251 !important; }
        :global(kbd) { outline: 1px solid #5e001f !important; }
        :global(samp) { outline: 1px solid #9c0033 !important; }
        :global(var) { outline: 1px solid #d90047 !important; }
        :global(mark) { outline: 1px solid #ff0053 !important; }
        :global(bdi) { outline: 1px solid #bf3668 !important; }
        :global(bdo) { outline: 1px solid #6f1400 !important; }
        :global(ruby) { outline: 1px solid #ff7b93 !important; }
        :global(rt) { outline: 1px solid #ff2f54 !important; }
        :global(rp) { outline: 1px solid #803e49 !important; }
        :global(span) { outline: 1px solid #cc2643 !important; }
        :global(br) { outline: 1px solid #db687d !important; }
        :global(wbr) { outline: 1px solid #db175b !important; }
        :global(icon-loader, icon-loader .loader) { outline: 1px solid rgb(23 219 190 / 0.7) !important; }
    }
    // @formatter:on
</style>